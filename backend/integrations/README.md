# Planned Service Integrations

Add CRM, email, calendar, analytics, payment, and messaging adapters here. Keep provider-specific SDK calls inside this folder and expose application-level functions through the API layer.

