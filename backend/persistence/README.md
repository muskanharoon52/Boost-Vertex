# Planned Persistence Layer

Add the database client, migrations, repositories, and lead/newsletter persistence logic here when the backend is introduced. Keep credentials server-side and load them through the deployment environment.

